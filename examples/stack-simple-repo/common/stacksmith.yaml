description: Credential-free invocation for the simple Stack example.

# Reuse the platform-owned configuration shared by the examples.
configs:
  - source: local
    data:
      path: ../../shared-config-repo/stacksmith-base-config.yaml
  - source: local
    data:
      path: ../../shared-config-repo/terraform-data-config.yaml
stacks:
  - source: local
    data:
      path: ../stack.yaml
