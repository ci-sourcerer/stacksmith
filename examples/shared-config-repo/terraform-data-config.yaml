description: Credential-free configuration for the terraform-data examples.

module_mappings:
  terraform_data:
    description: Credential-free placeholder resource
    source:
      source: local
      data:
        path: ../modules/terraform_data
    auto_inject: true
